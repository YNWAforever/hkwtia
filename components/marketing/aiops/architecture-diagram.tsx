import type {AiOpsDashboardLabels} from "./dashboard";
const webFlow = ["Web / WhatsApp", "Concierge runtime", "guarded tools", "Neon Postgres", "Cloudflare Worker"];
const scheduledFlow = ["authenticated job routes", "scheduled agents / AI-Ops refresh", "Neon Postgres"];
export function ArchitectureDiagram({labels}: Readonly<{labels: AiOpsDashboardLabels}>) { return <section className="glass-card space-y-5 p-6"><div><h2 className="font-serif text-2xl font-semibold">{labels.architectureHeading}</h2><p className="mt-2 text-muted-foreground">{labels.architectureDescription}</p></div><div className="grid gap-6 md:grid-cols-2"><ol className="space-y-2">{webFlow.map((node, index) => <li className="flex items-center gap-2" key={node}><span>{node}</span>{index < webFlow.length - 1 ? <span aria-hidden="true">→</span> : null}</li>)}</ol><ol className="space-y-2">{scheduledFlow.map((node, index) => <li className="flex items-center gap-2" key={node}><span>{node}</span>{index < scheduledFlow.length - 1 ? <span aria-hidden="true">→</span> : null}</li>)}</ol></div><div className="flex flex-wrap gap-3"><p className="rounded-full border px-3 py-1 text-sm">{labels.approvalGate}</p><p className="rounded-full border px-3 py-1 text-sm">{labels.publicationGate}</p></div></section>; }
