type Attendee = Readonly<{profileId: string; displayName: string; email: string | null; status: string; checkedInAt: Date | null}>;
type Labels = Readonly<{caption: string; name: string; email: string; status: string; checkedIn: string; checkIn: string; unavailable: string}>;

export function AttendeeTable({attendees, labels, checkInAction}: Readonly<{attendees: readonly Attendee[]; labels: Labels; checkInAction: (formData: FormData) => Promise<void>}>) {
  return <div className="overflow-x-auto"><table className="w-full text-left"><caption className="sr-only">{labels.caption}</caption><thead><tr><th className="p-3">{labels.name}</th><th className="p-3">{labels.email}</th><th className="p-3">{labels.status}</th><th className="p-3">{labels.checkedIn}</th><th className="p-3">{labels.checkIn}</th></tr></thead><tbody>{attendees.map((attendee) => <tr className="border-t" key={attendee.profileId}><td className="p-3">{attendee.displayName}</td><td className="p-3">{attendee.email ?? labels.unavailable}</td><td className="p-3">{attendee.status}</td><td className="p-3">{attendee.checkedInAt?.toISOString() ?? labels.unavailable}</td><td className="p-3"><form action={checkInAction}><input name="profileId" type="hidden" value={attendee.profileId}/><button className="underline disabled:no-underline" disabled={Boolean(attendee.checkedInAt)} type="submit">{labels.checkIn}</button></form></td></tr>)}</tbody></table></div>;
}
