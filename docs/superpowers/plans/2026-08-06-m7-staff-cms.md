# M7 Staff CMS Implementation Plan

> **Status:** M7.1 and M7.2 are shipped; the boxes below record what was
> delivered. M7.3 is not started. Design record:
> `docs/superpowers/specs/2026-08-06-m7-staff-cms-design.md`.

**Goal:** Let WTIA staff publish news and edit marketing page copy without a
developer, by adding two more instances of the admin CRUD stack the repo already
has rather than introducing a CMS as a new system.

**Architecture:** All Neon/Drizzle access stays inside `lib/db/repos`. News
reuses the existing `posts` table scoped to `kind: "news"`. Page copy adds one
`page_copy` table and merges at a single point in `i18n/request.ts`, with the
message bundles as the structural source of truth and the fallback.

**Tech Stack:** Next.js App Router, React Server Components, TypeScript strict,
next-intl, Drizzle ORM with Neon Postgres, Zod, Vitest, Playwright.

## Global Constraints

- Develop on `claude/code-review-icazin`; never point fixture or seed commands
  at Production — use an isolated `DATABASE_URL_TEST` per AGENTS.md.
- Every repository method takes an `Actor` first and calls `requireAdmin` before
  parsing and before opening a connection; denial becomes `notFound()`.
- Every mutation writes its audit row inside the same transaction.
- All visible copy lives in `messages/en.json` and `messages/zh-HK.json`.
- News owns `kind: "news"` only. `kind: "page"` stays the board reporter's and
  `kind: "buildlog"` stays seed/agent-owned; neither becomes editable.
- The `SafeStructuredContent` grammar is not extended. Each addition (images,
  external links, italics, ordered lists, code blocks) is a new XSS surface in
  the one component that renders untrusted body content and needs its own
  decision with its own hostile-input tests.
- Page-copy overrides may only replace an existing English leaf string, and the
  editable namespace set is a hard-coded allowlist in code.
- Preserve prior migrations; add only sequential `drizzle/0016_m7_page_copy.sql`
  plus its generated snapshot and journal entry.

---

### Task 1: News authoring (M7.1) — shipped in `bc06d11`

**Files:**
- Create: `lib/db/repos/admin-posts.ts`
- Create: `lib/admin/news-{actions,action-core,form-input}.ts`
- Create: `components/admin/news-form.tsx`
- Create: `app/[locale]/(admin)/admin/news/{page,[id]/page}.tsx`
- Create: `lib/news/revalidate.ts`
- Modify: `lib/db/repos/public-posts.ts`, both public news pages, `app/sitemap.ts`,
  `components/admin/admin-nav.tsx`, the admin layout, both message bundles
- Delete: `content/news.ts`, `lib/news/build-log-visibility.ts`,
  `components/marketing/news-detail.tsx`

- [x] **Step 1:** Staff CRUD scoped to `kind: "news"`, with injectable
  transaction dependencies, `.for("update")` row locking, and an explicit
  `posts_slug_unique` conflict surfaced as a slug field error.
- [x] **Step 2:** Publication modelled as a nullable instant behind a checkbox;
  re-saving keeps the original instant so the feed does not reorder.
- [x] **Step 3:** `post.published` / `post.unpublished` audit rows written in the
  same transaction as the update — the only record of who published.
- [x] **Step 4:** `listPublishedNews` / `getPublishedNewsBySlug` added
  **alongside** the build-log readers, never widening them, so news cannot leak
  into the public AI-Ops evidence panel.
- [x] **Step 5:** The static news path retired and both public news pages plus
  the sitemap pointed at the database.
- [x] **Step 6:** Registered in the three admin guard tests; the existing "no
  publish button" assertions on `/admin/reports`, `/admin/approvals` and the
  board-draft preview still pass untouched.

### Task 2: Page copy (M7.2) — shipped

**Files:**
- Create: `lib/i18n/{page-copy-scope,apply-page-copy,page-copy-catalog,page-copy-cache}.ts`
- Create: `lib/db/repos/page-copy.ts`
- Create: `lib/admin/page-copy-{actions,action-core,form-input}.ts`
- Create: `lib/admin/revalidate-public-path.ts`
- Create: `components/admin/page-copy-form.tsx`
- Create: `app/[locale]/(admin)/admin/page-copy/{page,[namespace]/page}.tsx`
- Create: `drizzle/0016_m7_page_copy.sql`, `drizzle/meta/0016_snapshot.json`
- Modify: `lib/db/schema-core.ts`, `i18n/request.ts`, `drizzle/meta/_journal.json`,
  `components/admin/admin-nav.tsx`, the admin layout, both message bundles

- [x] **Step 1:** `applyPageCopy` written as a pure path-walk over a structural
  clone — never a generic deep merge — so `Privacy.sections` stays an array and
  `t.raw()` consumers keep working. Tested against arrays explicitly.
- [x] **Step 2:** The 194-string, nine-namespace allowlist and the namespace →
  public-route map, with a test that every namespace maps to a declared route.
- [x] **Step 3:** `page_copy` table and migration; unique on
  `(locale, namespace, keyPath)`, nullable `updatedByProfileId` so removing a
  staff profile never deletes published copy.
- [x] **Step 4:** Repository with `requireAdmin` first, both locales saved in one
  transaction, diff-only writes, and a `page_copy.updated` audit row naming what
  changed. Unknown paths, out-of-scope namespaces, placeholder mismatches and
  duplicate keys are all rejected before anything is written.
- [x] **Step 5:** Fail-soft loader wired into `i18n/request.ts` with a
  process-local TTL cache; verified that removing the catch makes the test fail
  and that `next build` succeeds with `DATABASE_URL` unset.
- [x] **Step 6:** Admin section cloned from the news stack; editor fields
  generated from the English bundle, bundle value as placeholder, override as
  value, en/zh side by side, blank to revert.
- [x] **Step 7:** `revalidatePublicPath` guard against `config/public-routes.ts`,
  both locale variants, invalidated per namespace on save.
- [x] **Step 8:** Registered in the three admin guard tests; full gate set green.

### Task 3: Media (M7.3) — not started

**Files (planned):**
- Modify: `lib/db/schema-core.ts` (a `media` table), `next.config.ts`
- Create: `lib/db/repos/media.ts`, an admin section, migration `0017`

- [ ] **Step 1:** Media registry — `url`, `altEn`, `altZh`, `width`, `height`,
  `uploadedBy`, timestamps — with an admin section to register and browse
  images. Validate `url` with the existing `isSafeLogoReference` rule
  (site-relative or `https:`; rejects `javascript:`, `data:` and
  protocol-relative). Zero new dependencies.
- [ ] **Step 2:** Add explicit `images.remotePatterns` to `next.config.ts` —
  never a wildcard. `next/image` rejects every remote host today, and only
  `page-hero`, `program-grid` and `site-header` use it.
- [ ] **Step 3:** Upload via `@vercel/blob` behind the same registry interface,
  only if step 1 proves insufficient.

---

## Verification

Full AGENTS.md gate set after each milestone:

```sh
npm test && npm run lint && npm run typecheck && npm run audit:strings && npm run build
```

Milestone-specific checks, all covered by the suite:

- **M7.1** — a member and an anonymous actor get `FORBIDDEN` before the
  connection is opened; a published post appears on `/news`, at its slug and in
  the sitemap while a draft appears in none; a build log is not editable through
  the news UI; a duplicate slug is a field error, not a generic failure;
  publishing writes its audit row in the same transaction.
- **M7.2** — `applyPageCopy` replaces `Privacy.sections.0.body.0` and leaves
  `sections` an array; unknown key paths, out-of-scope namespaces and
  placeholder mismatches are each rejected; `getRequestConfig` returns the
  bundle unchanged when the repository throws and `npm run build` succeeds with
  no `DATABASE_URL`; every in-scope namespace maps to a real public route; an
  override changes one locale while the other still shows its bundle value.
- **M7.3** — `javascript:`, `data:` and protocol-relative URLs are rejected; a
  registered https image renders through `next/image` against the allowlist.

Remaining end-to-end, against an isolated `DATABASE_URL_TEST` — never point
fixture commands at production: sign in as staff, publish a news post, confirm
it on `/news` in both locales, edit a page-copy string, and confirm the public
page reflects it after revalidation.

## Known follow-ups

- The Chinese bundle still carries `"_review": true`; the whole translation set
  is pending human review, and page-copy overrides do not change that.
- The privacy notice needs legal review and carries no "last reviewed" date.
- Programme details, the chairman's message, committee names and remits, About
  copy, and the first news posts and events are all blocked on facts the
  association has to supply. The tooling to enter them now exists.
- `approvals.decide` records a sign-off but has **no effect executor** — nothing
  acts on `status = 'approved'`. M7 publishes directly, which is fine, but any
  future approval-gated publishing needs the effect built, not just an approval
  type registered.
