# WiseTech design fidelity — living checklist

Programme spec: `docs/superpowers/plans/2026-09-01-wisetech-design-fidelity.md` (errata in its Appendix D).
Overview picture: `docs/integration/wisetech-hkwtia-overview.png`.

This file is the spec's §6 acceptance table turned into a living checklist: one row per design section,
component or function the programme ports. The status column is the only field that changes between work
packages; rows are never deleted, so the table stays a complete record of what "perfectly integrated" means.

Statuses:

- `not started` — no code on `main` for the row.
- `ported` — the code exists on a branch or on `main` with RED/GREEN evidence in its PR, but the row's check has not yet passed in the owning WP's final gate.
- `verified` — the row's check passed in the owning WP's final gate and the PR link is recorded.

Rules (spec §5, §6):

- A work package is done only when every row it owns is `verified` with a PR link.
- A row may not skip `ported`.
- A row whose check needs an isolated Preview (Lighthouse, credential-gated e2e) is never marked `verified` from a local run alone; record the Preview URL in the evidence column.
- Flipping a row is part of the PR that earns it, in the same commit as the code or evidence.

## Work-package rows

WP-2 rows 2.1 to 2.10 are `verified` against pull request [#35](https://github.com/YNWAforever/hkwtia/pull/35),
whose command log carries the final gate: 3427 unit tests, 112 end-to-end cases across the five shell
suites plus the Concierge suite, all 72 visual baselines, and a clean lint, typecheck, build, dependency
audit and stylesheet drift check.

WP-3 rows 3.1 to 3.18 are `verified` on branch `feat/wt-wp2-shell`, which these commits land on top of
pull request [#35](https://github.com/YNWAforever/hkwtia/pull/35) (WP-2) — recorded here against #35
because that is the pull request this branch's commits actually update; if the branch is later split so
WP-3 ships under its own pull request, this column should be repointed then. Task 16's final gate carries
the verification: 3478 unit tests across 407 files, a clean lint (0 errors), typecheck, production build
and `npm run audit:strings` (194 files), plus a green run of `tests/e2e/wisetech-pr3-public-pages.spec.ts`
re-measured against the rebuilt hero for both locales at 375/768/1024/1440px. Row 3.19 (Lighthouse) stays
`ported`, not `verified`, per this file's own rule above: its check needs an isolated Preview, which this
task did not have, so a local `npm run test:lighthouse` pass is recorded as evidence rather than as the
verifying run.

| # | WP | Area | Item (donor pattern → hkwtia target) | Check | Status | PR | Evidence |
|---|---|---|---|---|---|---|---|
| 0.1 | WP-0 | Harness | `tests/e2e/wisetech-visual-baseline.spec.ts`: 9 routes × 4 viewports × 2 locales, reduced motion, `maxDiffPixelRatio` 0.02 | first run writes the baselines, a second run passes unchanged | verified | [#33](https://github.com/YNWAforever/hkwtia/pull/33) | PR #33 command log: full local gate, RED/GREEN e2e runs, review trail |
| 0.2 | WP-0 | Harness | Baselines committed under `tests/e2e/__screenshots__/wisetech-visual-baseline/` with the platform and project in the file name | files present, spec green against them | verified | [#33](https://github.com/YNWAforever/hkwtia/pull/33) | PR #33 command log: full local gate, RED/GREEN e2e runs, review trail |
| 0.3 | WP-0 | Docs | Spec committed as `docs/superpowers/plans/2026-09-01-wisetech-design-fidelity.md` with errata appendix; overview picture in `docs/integration/` | files present | verified | [#33](https://github.com/YNWAforever/hkwtia/pull/33) | PR #33 command log: full local gate, RED/GREEN e2e runs, review trail |
| 0.4 | WP-0 | Docs | `CLAUDE.md` pointer block (spec §0.4) and corrected unsubscribe sunset date | block present, date matches `lib/email/unsubscribe-token.ts` | verified | [#33](https://github.com/YNWAforever/hkwtia/pull/33) | PR #33 command log: full local gate, RED/GREEN e2e runs, review trail |
| 1.1 | WP-1 | Tokens | `--wt-*` colour, line, shadow and focus tokens with the exact donor values in `app/globals.css` (§4.1) | `tests/unit/wisetech-tokens.test.ts` | verified | [#34](https://github.com/YNWAforever/hkwtia/pull/34) | PR #34 command log: full local gate, RED/GREEN per task, axe re-run, review trail |
| 1.2 | WP-1 | Tokens | Retuned `--shell-*` and shadcn HSL triplets to the donor palette; names kept for `public-shell-tokens.test.ts` | `wisetech-tokens.test.ts`; `public-shell-tokens.test.ts` untouched and green | verified | [#34](https://github.com/YNWAforever/hkwtia/pull/34) | PR #34 command log: full local gate, RED/GREEN per task, axe re-run, review trail |
| 1.3 | WP-1 | Typography | `--font-serif` Iowan Old Style / Palatino Linotype stack, `--font-sans` Avenir Next stack plus zh fallbacks; `Playfair_Display` / `Inter` removed from `app/[locale]/layout.tsx` (D-2); `h1,h2` weight 500, tracking -0.045em | `wisetech-tokens.test.ts`; e2e request filter on `fonts.gstatic.com` = 0 | verified | [#34](https://github.com/YNWAforever/hkwtia/pull/34) | PR #34 command log: full local gate, RED/GREEN per task, axe re-run, review trail |
| 1.4 | WP-1 | Tokens | `tailwind.config.ts`: `colors.wt.*`, `screens` wt-xl/lg/md/sm (1320/1120/820/520), `maxWidth.shell` 1440px, font families | `wisetech-tokens.test.ts` | verified | [#34](https://github.com/YNWAforever/hkwtia/pull/34) | PR #34 command log: full local gate, RED/GREEN per task, axe re-run, review trail |
| 1.5 | WP-1 | CSS port | `app/styles/wisetech.css`: selective donor rules, no `@import "tailwindcss"`, no `url(http`, no donor route strings, pinned selectors present, imported after the Tailwind layers | `tests/unit/wisetech-css-port.test.ts` | verified | [#34](https://github.com/YNWAforever/hkwtia/pull/34) | PR #34 command log: full local gate, RED/GREEN per task, axe re-run, review trail |
| 1.6 | WP-1 | Primitives | `components/wt/`: shell, section, section-heading, eyebrow, status-label, arrow, card-index, honest-empty, page-hero, closing-band, interest-band, step-grid, card-grid, page-updated, action-link, types (Server Components, `Readonly` props, no strings inside) | `tests/unit/wt-primitives.test.tsx` | verified | [#34](https://github.com/YNWAforever/hkwtia/pull/34) | PR #34 command log: full local gate, RED/GREEN per task, axe re-run, review trail |
| 1.7 | WP-1 | Primitives | `components/ui/button.tsx` variants `wt`, `wtDark`, `wtLight`, `wtText` | `wt-primitives.test.tsx` | verified | [#34](https://github.com/YNWAforever/hkwtia/pull/34) | PR #34 command log: full local gate, RED/GREEN per task, axe re-run, review trail |
| 1.8 | WP-1 | Motion | `hero-breathe`, `node-pulse`, `pulse-ring` keyframes inside `prefers-reduced-motion: no-preference`; existing `reduce` block kept | `wisetech-css-port.test.ts` | verified | [#34](https://github.com/YNWAforever/hkwtia/pull/34) | PR #34 command log: full local gate, RED/GREEN per task, axe re-run, review trail |
| 1.9 | WP-1 | Harness | Baselines regenerated at the end of WP-1 (colour / type drift only) and committed | visual baseline spec green | verified | [#34](https://github.com/YNWAforever/hkwtia/pull/34) | PR #34 command log: full local gate, RED/GREEN per task, axe re-run, review trail |
| 2.1 | WP-2 | Shell | Announcement bar: ink bar, amber dot, link with arrow, dismiss button with `aria-label`, per-session dismissal; data still `announcementsRepository.getActive` | `tests/unit/public-layout-announcement.test.tsx` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `c90dd76`, `d154a18`, `ecfebae`: `tests/unit/announcement.test.tsx` and `tests/unit/public-layout-announcement.test.tsx` pin the donor `.announcement` markup, the dismiss `aria-label` and the per-session dismissal; the wrap declaration jsdom cannot compute is pinned in `tests/unit/wisetech-css-port.test.ts` instead (errata E-66). No announcement appears in any browser run or baseline, because the managed server maps `DATABASE_URL` to `DATABASE_URL_TEST` (errata E-7) — which is why the skip-link defect in E-60 could only be found by reading. Errata E-41, E-42, E-43, E-60, E-66 |
| 2.2 | WP-2 | Shell | Site header: `overlay` / `solid` variant, `.scrolled` at scrollY > 56, `.no-announcement`, 3-column grid, brand lockup, triggers with `⌄`, actions (search, language, Member sign in, Join WiseTech), mobile trigger | `tests/unit/public-shell.test.tsx`; `tests/e2e/public-shell.spec.ts` scrolled assertion | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `11a4b49`, `2c48ada`, `a15b7be`, `1f5962b`: `tests/unit/public-shell.test.tsx` and `tests/unit/hero-variant.test.tsx` pin the variant, the `.no-announcement` modifier and the header slots in donor order, by their exact markers rather than by fallbacks (errata E-66); `tests/e2e/public-shell.spec.ts` asserts the scrolled header. The overlay header's locale control and the current-section underline took companion rules in `1f5962b`, pinned in `tests/unit/wisetech-css-port.test.ts`. Errata E-13, E-14, E-15, E-16, E-25, E-33, E-47, E-61, E-64 |
| 2.3 | WP-2 | Shell | Desktop mega menu: "Explore <group>" heading, "View overview ↗", titled columns, feature aside per group from `config/navigation.ts` (`PublicRoute` hrefs); keyboard contract and timers preserved (D-3, 4 canonical groups) | `tests/unit/navigation.test.ts`, `public-shell.test.tsx`, `public-shell.spec.ts` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `2f1473b`, `4ade247`, `2314f01`, `4246913`, `ecfebae`: `tests/unit/navigation.test.ts`, `tests/unit/navigation-feature.test.ts`, `tests/unit/public-shell.test.tsx` (the current-leaf case now asserts the marked element *is* the named one) and `tests/unit/desktop-mega-navigation.test.tsx`; `tests/e2e/public-shell.spec.ts` covers traversal, Escape and focus return at 1360 px. The panel now closes on a history navigation as well as on a link click. Errata E-17, E-27, E-36, E-37, E-38, E-61, E-65, E-66 |
| 2.4 | WP-2 | Shell | Mobile dialog: top bar, priority actions, utilities, "Explore the ecosystem" accordions with view-all, focus trap, scroll lock, closes after locale switch | `tests/unit/mobile-navigation.test.tsx`, `public-shell.spec.ts` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `10ef644`, `b261edb`, `4246913`: `tests/unit/mobile-navigation.test.tsx` (priority actions, utilities, and the five-leaf cap with the group landing appended as the `.mobile-view-all`) and `tests/e2e/public-shell.spec.ts`. The cap is on leaves, not on links: only Events overflows it, at six leaves sliced to five, and About renders exactly five, so those two groups show six links while Membership shows three and Insights four. The case exercises Events, the only group where the slice does anything. The current-group contrast is guarded by a computed-ratio case in `tests/e2e/accessibility.spec.ts`, because axe cannot score text over the donor gradient; the dialog also closes on a history navigation, deliberately without pulling focus back to the hamburger. Errata E-19, E-31, E-34, E-35, E-39, E-40, E-45, E-65 |
| 2.5 | WP-2 | Shell | Brand lockup tile (108×48) with `public/images/wtia-logo.png` and the D-10 descriptor | `tests/unit/wisetech-asset-provenance.test.ts` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `11a4b49`, `4ade247`: `tests/unit/dual-brand-lockup.test.tsx` and `tests/unit/wisetech-asset-provenance.test.ts`. The descriptor's opacity correction over the port is errata E-30 |
| 2.6 | WP-2 | Shell | Footer: brand + description + legal line, newsletter prepared-`mailto:` form with `role="status"` / `role="alert"` (D-6), 4 link columns, `<address>` from the message bundle, bottom row | footer snapshot test | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `2e1341a`, `3642e45`, `50259a6`, `1f5962b`: `tests/unit/site-footer.test.tsx` (four columns, the English and Chinese `<address>`, the newsletter status and alert, the always-rendered mail link, five bad `mailBody` shapes plus a control, the bottom row) plus the 20-href footer sweep in `tests/unit/public-shell.test.tsx`, which compares sorted multisets so a swapped href is named rather than counted (errata E-66). The `<address>` prints `Footer.addressLines`, not `config/site.ts` (errata E-68). Errata E-20 (phone unset, and the contact page still printing one), E-21 (About column), E-22, E-32, E-33, E-48, E-62, E-65, E-66, E-68 |
| 2.7 | WP-2 | Shell | Concierge launcher restyled to `W+` "Ask WiseTech", with per-section prompt labels resolved from the route; the donor panel skin is out of scope (errata E-18 defers it to WP-6) | `tests/unit/concierge-widget.test.tsx`, `tests/e2e/concierge.spec.ts` unchanged and green | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `b3bb178`, `5cc13d5`, `da552bd`, `2525d34`: `tests/unit/concierge-shell.test.tsx` pins the section following a soft navigation, the chips clearing on the first turn and the launcher's label in both bundles; `tests/unit/concierge-widget.test.tsx` and `tests/e2e/concierge.spec.ts` are untouched (`git diff --exit-code`) and green. The launcher renders inside the member portal too, where the port is not loaded and the Tailwind utilities carry it. Errata E-18, E-23, E-44, E-46, E-65 |
| 2.8 | WP-2 | Shell | `app/[locale]/(public)/layout.tsx` wraps `div.site-root` with the `zh-Hant-HK` lang; header variant from `lib/public-shell/hero-variant.ts` | `public-shell.test.tsx` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `c90dd76`, `11a4b49`, `b3bb178`, `1f5962b`: variant assertions in `tests/unit/public-shell.test.tsx` and `tests/unit/hero-variant.test.tsx`, and the layout's owner order — skip link, bar, header, main, footer, widget — pinned by its exact markers. `div.site-root` with the `zh-Hant-HK` lang was delivered in WP-1 (`app/[locale]/(public)/layout.tsx:49`); WP-2 adds the header variant, the companion stylesheet import and the Concierge props, and `1f5962b` lifts the skip link out from under the announcement bar. Errata E-13, E-24, E-60 |
| 2.9 | WP-2 | i18n | `Navigation.feature.*`, `Navigation.explore` / `viewOverview` / `search`, `Footer.newsletter.*`, `Footer.tagline`, `Concierge.prompts.*` in `en` and `zh-HK` parity | `tests/unit/messages.test.ts`, `npm run audit:strings` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `2a57d62`, `1cf2b43`, `2e1341a`, `1f492f9`, `4158b40`, `a6ce368`: `tests/unit/messages.test.ts` parity, `tests/unit/navigation-feature.test.ts`, `tests/unit/concierge-prompts.test.ts`, and `npm run audit:strings` exit 0 across 185 TSX files. `4158b40` repairs four Hong Kong Chinese copy defects that key parity cannot see, leaving `messages/en.json` untouched. Errata E-26, E-28, E-57 (the orphaned `Navigation.groups.*.description`, swept in Task 9) and E-69 |
| 2.10 | WP-2 | Gate | Shell boundary green (no donor imports or donor-only paths); axe clean; baselines updated at 1440 / 1120 / 820 / 520 | `tests/unit/wisetech-shell-boundary.test.ts`, `tests/e2e/accessibility.spec.ts`, visual baseline | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `ecfebae`, `2525d34`, `3777814`: `tests/unit/wisetech-shell-boundary.test.ts` scans **nineteen** shell files with no donor reference — the list read eleven and omitted seven of this work package's own files until `ecfebae` (errata E-53, E-63); `tests/e2e/accessibility.spec.ts` reports 0 serious or critical across the public matrix; all 72 visual baselines were captured once, at the branch tip, at 1440 / 1120 / 820 / 390 (the harness's fourth viewport is 390, errata E-26). The branch carries exactly one capture: a superseded mid-branch one was folded away before publication, which is why hashes from the twenty-first commit onward differ from the ones quoted while the work was in flight (errata E-67). Errata E-25, E-34, E-49, E-50, E-51, E-52, E-53, E-54, E-63, E-66, E-67 |
| 3.1 | WP-3 | Home | 1 · Hero (`components/home/hero.tsx`): `Home.hero.*`, own-origin image with `fetchPriority="high"`, scrim, network field | `tests/unit/homepage.test.tsx` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `291fbbb`: hero with the donor's top-spanning scrim over `public/images/projects-hero.jpg`. Task 16 re-measured `#home-discover`'s viewport ratio against this hero (E-52, Appendix D row E-70) and the overlay header's focus-ring contrast against it (E-64, row E-70); both hold |
| 3.2 | WP-3 | Home | 2 · Open now: `eventsRepository.listPublic(anonymous, {status:"open", limit:3})` `.catch(() => [])`; honest empty + interest band + "Submit a challenge" | `homepage.test.tsx` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `01baf6b`: Open Now section over real open events, `#home-discover`'s anchor target |
| 3.3 | WP-3 | Home | 3 · Pathways: 5 static cards with audience accents | `homepage.test.tsx` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `0352d5f`: pathways section with the D-7 audience cards |
| 3.4 | WP-3 | Home | 4 · Events journey: `listFeaturedPublic(anonymous, {limit:2})`, stage grid always, `.event-empty` fallback | `homepage.test.tsx` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `017108c`: events-journey section over featured public events |
| 3.5 | WP-3 | Home | 5 · Directory + Marketplace: `showcaseRepository.listPublished({}, {limit:12})` length, bounded, never a total | `homepage.test.tsx` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `167d5fd`: market-products section over showcase presence |
| 3.6 | WP-3 | Home | 6 · Outcomes: honest publishing-framework state (D-8) | `homepage.test.tsx` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `c3b6f59`: outcomes section as an always-honest empty state |
| 3.7 | WP-3 | Home | 7 · AI+ Ecosystem: 6 industries, client selector with list passed in, links per D-4 | `homepage.test.tsx` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `1e0b5d0`: ecosystem section with the D-4 industry selector |
| 3.8 | WP-3 | Home | 8 · Programme showcase: `content/programs/index.ts` typed records | `homepage.test.tsx` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `db33a36`: programme-showcase section over typed programme records |
| 3.9 | WP-3 | Home | 9 · GBA gateway: open-cohort flag from `cohortRepository` (`PUBLIC_COHORT_STATUSES`) | `homepage.test.tsx` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `1cd3a9b`: GBA gateway section with the open-cohort CTA |
| 3.10 | WP-3 | Home | 10 · Impact evidence: `lib/home/impact-metrics.ts` with definition + period + source per tile, `Promise.allSettled`, zero or failed tiles omitted, section hidden when none survive | `tests/unit/impact-metrics.test.ts` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `edd4213`, `afa2dc3`: impact-evidence section with computed metrics (D-8), past-events tile backed by a real count query |
| 3.11 | WP-3 | Home | 11 · Archive stories: `featuredOnly(milestones)` top 4 with `content/milestone-image-map.json` images (D-9); hidden below 1 image | `homepage.test.tsx` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `b09b0fb`, `7d0a4f3`: archive-stories section over featured milestones (D-9) |
| 3.12 | WP-3 | Home | 12 · Legacy network: `partnersRepository.listPublished(locale, {limit:100})` grouped supporting / regional / media, 12 per tab, hidden at 0, no hard-coded 79 | `tests/unit/home-legacy-network.test.tsx`, `tests/unit/legacy-network-groups.test.ts` (this row's original `wisetech-no-fabricated-metrics.test.ts` name was the spec's anticipated name; the implementation landed under these two instead) | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `07e7220`, `07b5299`: legacy-network section replacing the partner wall, with a11y and robustness fixes |
| 3.13 | WP-3 | Home | 13 · Conversion paths (static) | `homepage.test.tsx` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `d8e63e1`: conversion-paths section (membership + partnership) |
| 3.14 | WP-3 | Home | `app/[locale]/(public)/page.tsx` is a thin composition; 13 landmarks in donor order with donor ids; `lib/home/home-highlights.ts` retained | `homepage.test.tsx` (rewritten), `home-highlights*.test.ts` | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `4ab1204`: composes the 13-section homepage; `125d93f` extracts the label-builders the composition reuses |
| 3.15 | WP-3 | SEO | Metadata: absolute title `WiseTech Hong Kong`, donor description via `Home.metaTitle` / `Home.metaDescription`, own-origin `public/images/og-wisetech.png` | homepage metadata test | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `app/[locale]/(public)/page.tsx:32` builds metadata from `Home.metaTitle` / `Home.metaDescription` with the hero image; carried through `4ab1204`'s composition unchanged |
| 3.16 | WP-3 | Config | `config/site.ts` `contact` block (email, phone verified against hkwtia.org, address lines) read by footer, contact page and structured data | footer / contact tests | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `1cf87e1`: confirms the WTIA phone number and reads it from config on both surfaces; `8d69c68` names the two address authorities and pins the Chinese one (errata E-68) |
| 3.17 | WP-3 | SEO | `buildOrganizationData()` gains `alternateName`, `telephone`, `address`; `WebSite` node with `inLanguage` | structured-data test | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | `4ab1204`: extends structured data alongside the composition (errata E-68); `tests/unit/structured-data.test.ts` |
| 3.18 | WP-3 | Gate | `homepage-partner-wall-integration.test.tsx` migrated; `wisetech-pr3-public-pages.spec.ts` updated for the new H1 | unit + e2e | verified | [#35](https://github.com/YNWAforever/hkwtia/pull/35) | Task 16 rewrote `tests/e2e/wisetech-pr3-public-pages.spec.ts`'s homepage fixtures and width sweep for the WP-3 hero (this row's original `homepage-partner-wall-integration.test.tsx` name predates the legacy-network rewrite of row 3.12; partner-wall coverage lives in `home-legacy-network.test.tsx` instead). Full local gate green: 3478 unit tests / 407 files, lint, typecheck, build, `audit:strings`, and the rewritten e2e spec across both locales at 375/768/1024/1440px. Task 16 also found and scoped-around a real gap outside this row's own check: the hero's `.hero-scroll` Discover indicator is unreachable below 820px (donor media query) — see the final report and the note on row 3.1 |
| 3.19 | WP-3 | Performance | `/` added to `lighthouserc.js`; Performance ≥ 90 with the hero as LCP; CLS < 0.05 | `npm run test:lighthouse` on an isolated Preview | ported | | Task 16 added `/` and `/zh` to `lighthouserc.js`'s collect targets and ran `npm run test:lighthouse` locally: performance/accessibility/seo cleared the bar on all four URLs (`/`, `/zh`, `/membership`, `/zh/membership`). Per this file's own rule, a local run does not verify a row that needs an isolated Preview — stays `ported` until run against one |
| 4.1 | WP-4 | Pages | `/events`: inner hero, quick tabs Open Now / Past Events only (D-5), activity strip to canonical routes, results count `role="status"`, cards ↔ calendar toggle (`?view=calendar`), donor EventCard, recommendations row, interest band, closing band | `tests/unit/wt-pages/events.test.tsx`, `tests/e2e/wisetech-pr5-public-journeys.spec.ts` | not started | | |
| 4.2 | WP-4 | Pages | `/events/[slug]`: detail hero (PR5 `0023` media), facts, main/aside layout, action bar with the real registration form or "Past event" state, `Event` JSON-LD | `tests/unit/event-detail-seo.test.ts`, `event-*` suites | not started | | |
| 4.3 | WP-4 | Pages | `/showcase`: page hero, prompts (`?q=`), search bound to `ShowcaseFilters`, 12 need chips (`?useCase=`), restyled cards, `HonestEmpty`, "Proposed badge definitions — not currently awarded", buyer / provider pathways, interest band | `tests/unit/wt-pages/showcase.test.tsx`, `m5-*` suites | not started | | |
| 4.4 | WP-4 | Pages | `/showcase/[slug]`: existing detail + intro form restyled to the inner-page grammar (donor pending-record page not used) | `m5-*` suites | not started | | |
| 4.5 | WP-4 | Pages | `/membership`: plan grid over the 4 canonical plans with anchors and `/join?plan=`, SME pathway card (D-7), pricing note from the catalog, 12 dimensions, first-90 steps | `tests/unit/wt-pages/membership.test.tsx`, `membership-*` suites | not started | | |
| 4.6 | WP-4 | Pages | `/about`, `/about/history`, `/about/chairman`, `/about/committees`: rich-page grammar over the existing intro / story / timeline primitives | `tests/unit/wt-pages/about.test.tsx` | not started | | |
| 4.7 | WP-4 | Pages | `/programs/{tct,asa,hkict,cpai}`: programme record header + existing editions / credential; "Ask the programme team" `mailto:` (D-6) | `tests/unit/wt-pages/programs.test.tsx` | not started | | |
| 4.8 | WP-4 | Pages | `/launchpad`: route board + descriptive service grid, then the real cohort calendar, funding wizard and application form | `m6-*` suites | not started | | |
| 4.9 | WP-4 | Pages | `/news`, `/news/[slug]`: category chips only for categories that exist in `posts`, research-quality section, subscribe band (D-6) | `tests/unit/wt-pages/news.test.tsx` | not started | | |
| 4.10 | WP-4 | Pages | `/contact`: 6 enquiry routes, verified details from `siteConfig.contact`, Concierge launcher, prepared-email form with `?topic=` presets, no persistence | `tests/unit/wt-pages/contact.test.tsx` | not started | | |
| 4.11 | WP-4 | Pages | `/ai-transparency`, `/ai-ops`, `/privacy`: page hero + existing content; `/ai-ops` cards in `.impact-metrics` styling | `tests/unit/wt-pages/*.test.tsx` | not started | | |
| 4.12 | WP-4 | Pages | `app/[locale]/not-found.tsx`: donor not-found page (inner hero, "Go to the homepage" / "Find an event") | `tests/unit/wt-pages/not-found.test.tsx` | not started | | |
| 4.13 | WP-4 | Contract | Every public route uses `components/wt/page-hero.tsx` with an own-origin figure, caption and breadcrumb; exactly one `main#main-content` and a skip link | `tests/unit/public-landmark-contract.test.ts` | not started | | |
| 4.14 | WP-4 | Gate | `public-route-matrix.spec.ts` and `wisetech-pr5-public-journeys.spec.ts` selectors updated; `event-*`, `m5-*`, `membership-*` suites green | unit + e2e | not started | | |
| 5.1 | WP-5 | Content | `scripts/import-wisetech-partners.ts`: refuses without `WISETECH_PARTNER_IMPORT=true` plus sentinel or the owner's production pair; R2 upload; media + partner rows in one transaction, unpublished and unconfirmed; `partner.created` audit row; idempotent on `(category, name_en)`; prints counts only | `tests/unit/wisetech-partner-import.test.ts` | not started | | |
| 5.2 | WP-5 | Content | Staff runbook `docs/integration/wisetech-partner-import-runbook.md` (confirm relationship window + logo rights, then publish) | review | not started | | |
| 5.3 | WP-5 | Assets | Archive photography stays `retire` until rights are recorded; approved images enter via `/admin/media` with EN/ZH alt | `tests/unit/wisetech-asset-provenance.test.ts` | not started | | |
| 5.4 | WP-5 | Content | Page-copy allowlist leaves for staff-editable marketing strings, shipped copy seeded (M7.2 fail-soft) | page-copy tests | not started | | |
| 5.5 | WP-5 | Assets | Each `retire` → `merge` flip in `config/wisetech-authoritative-source-inventory.ts` in its own commit with the confirmation reference | provenance test | not started | | |
| 6.1 | WP-6 | Functions | `/[locale]/member-login` page + action on the existing rate-limited magic-link path | `tests/e2e/member-login.spec.ts` (credential-free) | not started | | |
| 6.2 | WP-6 | Functions | Operable Portal sign-out control | `tests/e2e/portal-dashboard.spec.ts` | not started | | |
| 6.3 | WP-6 | Functions | One typed authority for safe Portal continuation paths | PR6 RED tests | not started | | |
| 6.4 | WP-6 | Functions | Onboarding hand-off carries the paid membership id + checkout command: profile → company → checkout → review / complete | `join-*`, `checkout-*` suites | not started | | |
| 6.5 | WP-6 | Functions | Completion page reads the webhook-authoritative membership state | PR6 tests | not started | | |
| 6.6 | WP-6 | Functions | Locale-correct Billing Portal return URL | PR6 tests | not started | | |
| 6.7 | WP-6 | Shell | `components/app-shell/*` Join / Portal / Admin variants on WP-1 tokens; no public chrome inside authenticated layouts; axe on `/portal` and `/admin` | PR6 tests, credential-gated e2e | not started | | |
| 7.1 | WP-7 | Routes | `/partners` un-retired: real page (source note, category nav with counts, record cards with relationship status), `config/public-routes.ts`, sitemap, Connect column | `tests/unit/wisetech-route-parity.test.ts` + parity doc in the same commit | not started | | |
| 7.2 | WP-7 | Routes | `/programmes` un-retired as a typed index; `/programmes/{tct,asa,hkict,cpai,launchpad}` redirect | route-parity test | not started | | |
| 7.3 | WP-7 | Routes | 51 `merge` sitemap routes and the `merge` dispatcher patterns as `next.config.ts` redirects (`permanent: false`) generated from `config/wisetech-redirects.ts`; parity doc's "merge 45" corrected | `tests/unit/redirects.test.ts`, route-parity test | not started | | |
| 7.4 | WP-7 | Routes | `/search`, `/accessibility`, `/terms`, retired portal paths stay retired | route-parity test | not started | | |
| 7.5 | WP-7 | SEO | Sitemap `alternates.languages` per route; titles `<Page> \| WiseTech Hong Kong`; `noindex` for portal / join / unsubscribe | `seo-routes.test.ts`, `sitemap.test.ts`, `page-indexability.test.ts` | not started | | |
| 8.1 | WP-8 | Evidence | Full local suite plus `npm audit --omit=dev --audit-level=high` | command log in the PR | not started | | |
| 8.2 | WP-8 | Evidence | Browser suite on an isolated Preview: public matrix, shell, axe, concierge, PR3 / PR5 journeys, visual baseline | Preview URL + totals | not started | | |
| 8.3 | WP-8 | Performance | Lighthouse `/`, `/membership`, `/events` on the Preview: Performance ≥ 90, Accessibility ≥ 95, SEO ≥ 95, LCP < 2.5 s, CLS < 0.05 | report location | not started | | |
| 8.4 | WP-8 | i18n | Key parity + manual `/zh` walk of the 13 home sections and every WP-4 page | walk record | not started | | |
| 8.5 | WP-8 | Evidence | This checklist all `verified`; `docs/integration/wisetech-design-fidelity-evidence.md` with before / after screenshots per breakpoint and locale | review | not started | | |
| 8.6 | WP-8 | Owner | Owner actions recorded, not performed: `quality` required on `main` and `release`, isolated Neon + test identities, UAT owner, production approval, unsubscribe Phase B on or after 2026-09-10 | `docs/integration/wisetech-delivery-gates.md` | not started | | |
| 8.7 | WP-8 | Owner | Donor archived: README note in `YNWAforever/wisetech` pointing here; repo read-only | review | not started | | |

## §6 acceptance criteria

| Area | Criterion | How it is checked | Status |
|---|---|---|---|
| Tokens | Every `--wt-*` token equals the donor value; `--shell-*` retuned per §4.1; no Playfair / Inter font requests | `wisetech-tokens.test.ts`; Playwright request filter on `fonts.gstatic.com` = 0 | not started |
| Typography | `h1` on `/` computes to the serif stack at `clamp(60px,7.7vw,128px)`, `line-height: 0.9`; eyebrows 11px / 800 / 0.2em uppercase | computed-style assertions in `wisetech-visual-baseline.spec.ts` | not started |
| Layout | `.shell` width and `.section` padding match the donor at 1440 / 1120 / 820 / 520; no horizontal scroll at 390px | e2e `scrollWidth <= innerWidth` | not started |
| Home | 13 sections in donor order with donor ids; data-backed sections render real rows or the honest empty state; no hard-coded numbers | `homepage.test.tsx`, `impact-metrics.test.ts`, `wisetech-no-fabricated-metrics.test.ts` | not started |
| Shell | Overlay header over photo heroes, solid on scroll; mega menu with feature aside; mobile dialog with priority actions; 4-column footer; `W+` concierge pill | `public-shell.test.tsx`, `public-shell.spec.ts`, screenshots | not started |
| Inner pages | Every public route uses `components/wt/page-hero.tsx`; exactly one `main#main-content` and a skip link | `public-landmark-contract.test.ts`, `wt-pages/*.test.tsx` | not started |
| Journeys | Event registration, cohort application and showcase introduction still submit through their Server Actions; `/join` → checkout → completion works with the webhook-authoritative state; `/member-login` and portal sign-out exist | `event-*`, `m5-*`, `m6-*`, `join-*`, `checkout-*` suites + PR6 e2e | not started |
| i18n | `en` / `zh-HK` key parity; `/zh` renders every new section; no `/zh-HK/` href; `audit:strings` clean | `messages.test.ts`, `locale-href-boundary.test.ts`, `npm run audit:strings` | not started |
| Content | Partners imported unpublished with logos and bilingual alt; published only after both confirmations; home wall and `/partners` show only published rows | `wisetech-partner-import.test.ts`, `partner-*` suites | not started |
| Routes / SEO | All 51 `merge` donor sitemap routes redirect; `/partners` and `/programmes` are real pages; sitemap carries `hreflang` pairs; portal / join `noindex` | `redirects.test.ts`, `wisetech-route-parity.test.ts`, `sitemap.test.ts`, `page-indexability.test.ts` | not started |
| Accessibility | axe: 0 serious / critical on the public matrix; mega-menu roving focus, `Escape`, mobile focus trap; reduced motion honoured | `accessibility.spec.ts`, `public-shell.spec.ts` | not started |
| Performance | Lighthouse `/`, `/membership`, `/events` on Preview: Performance ≥ 90, Accessibility ≥ 95, SEO ≥ 95; LCP < 2.5 s; CLS < 0.05 | `npm run test:lighthouse` with extended targets | not started |
| Security | CSP unchanged (`img-src 'self' data:`); no donor runtime; no env access outside `lib/config/env.ts`; `npm audit --omit=dev --audit-level=high` clean | `ci-security-contract.test.ts`, `endpoint-hardening.test.ts`, `wisetech-shell-boundary.test.ts` | not started |
| Evidence | PR descriptions carry commands, exit codes and focused-test totals; the evidence doc has before / after screenshots per breakpoint and locale | review | not started |
